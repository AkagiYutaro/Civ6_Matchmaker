services:
  - type: worker
    name: civ6-matchmaker-bot
    runtime: docker
    envVars:
      # トークン類はGitHubに書かず、後でRenderの画面で設定します
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: SPREADSHEET_KEY
        sync: false
      - key: CREDS_FILE
        sync: false
